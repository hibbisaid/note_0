import 'package:flutter/material.dart';

import 'welcom.dart';

class body extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Column(
      children: [
        welcom ( name: '******',avatar:'assets/icons/hand_emoji.png'),
       // AddTaskButton(),
        //Task_list(),

      ],
    );   //column
  }
}
